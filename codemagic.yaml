workflows:
  ios_unsigned_release:
    name: iOS Unsigned IPA (Release)
    max_build_duration: 60
    environment:
      flutter: stable
      xcode: latest
      groups:
        - supabase
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: ".*"
          include: true
          source: true
    scripts:
      - name: Flutter clean & pub get
        script: |
          flutter clean
          flutter pub get
      - name: CocoaPods install with repo update
        script: |
          cd ios
          pod install --repo-update
          cd ..
      - name: Build IPA without codesign (Release + env vars)
        script: |
          set -e
          flutter build ipa --no-codesign --release \
            --dart-define=SUPABASE_URL=$SUPABASE_URL \
            --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY \
            --dart-define=SUPABASE_SERVICE_ROLE_KEY=$SUPABASE_SERVICE_ROLE_KEY
      - name: "Fallback: package IPA if missing"
        script: |
          set -e
          if ls build/ios/ipa/*.ipa 1> /dev/null 2>&1; then
            echo "IPA exists"
          else
            echo "IPA missing, trying to package Runner.app"
            APP_PATH=""
            if [ -d build/ios/archive/Runner.xcarchive/Products/Applications/Runner.app ]; then
              APP_PATH="build/ios/archive/Runner.xcarchive/Products/Applications/Runner.app"
            elif [ -d build/ios/iphoneos/Runner.app ]; then
              APP_PATH="build/ios/iphoneos/Runner.app"
            else
              APP_PATH=$(find build -type d -name Runner.app | head -n 1)
            fi
            if [ -z "$APP_PATH" ]; then
              echo "Runner.app not found; skipping fallback"
              exit 1
            fi
            mkdir -p build/ios/ipa/Payload
            cp -R "$APP_PATH" build/ios/ipa/Payload/Runner.app
            cd build/ios/ipa
            zip -r Runner.ipa Payload
            rm -rf Payload
          fi
      - name: Verify IPA artifact
        script: |
          set -e
          ls -lah build/ios/ipa || true
    artifacts:
      - build/ios/ipa/*.ipa
      - build/**/**/*.ipa
    publishing:
      email:
        recipients:
          - your@email.com
        notify:
          success: true
          failure: true

  android_release:
    name: Android APK (Release)
    max_build_duration: 60
    environment:
      flutter: stable
      vars:
        ANDROID_HOME: /opt/android-sdk
      groups:
        - supabase
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: ".*"
          include: true
          source: true
    scripts:
      - name: Flutter clean & pub get
        script: |
          flutter clean
          flutter pub get
      - name: Build Android Release APK (env vars)
        script: |
          set -e
          flutter build apk --release \
            --dart-define=SUPABASE_URL=$SUPABASE_URL \
            --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY
      - name: List APK artifacts
        script: |
          ls -lah build/app/outputs/flutter-apk/ || true
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
      - build/**/**/app-release.apk
